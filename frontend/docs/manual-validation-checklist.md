# 手工验证清单（移动端与桌面端）

## 范围

- 变更：`add-bank-repayment-webapp`
- 目标：验证首页、录入流程、完成页、历史页在移动端与桌面端可用

## 环境

- 前端：`npm run dev`
- 后端：本地 API 服务已启动并可访问 `/api/repayments/*`
- 视口：
  - 移动端：`390 x 844`
  - 桌面端：`1440 x 900`

## 检查项

- [x] 首页显示两个入口按钮：`记录还款`、`查看历史记录`
- [x] 首页显示“本月已还款总额”，且 RMB/HKD 分列
- [x] 步骤 1 为全屏卡片选择，未选卡点击“下一步”出现必填提示
- [x] 步骤 2 支持正负金额，超过两位小数时报错
- [x] 步骤 2 输入负数时显示 `绝对值 + CR` 预览
- [x] 步骤 3 默认当前本地时间（分钟精度），可通过拨盘修改
- [x] 步骤切换存在统一动画，移动端无明显卡顿
- [x] 完成页显示摘要信息，并可点击 `再记一笔` 与 `查看历史`
- [x] 历史页按月份分组，可展开/收起
- [x] 历史页筛选（银行卡/币种）会同时影响明细和统计区域
- [x] 历史页显示月总额和平均月开销公式标签
- [x] 所有负金额展示均为 `绝对值 + CR`
- [x] 删除必须弹出确认框，且确认框展示银行卡/金额/时间
- [x] 删除确认后记录消失，刷新后仍保持删除结果
- [x] 移动端和桌面端布局均未出现重叠、裁切或不可点击控件

## 结果记录

- 执行日期：2026-07-02
- 执行人：Codex（agent team + browser validation）
- 结果：通过
- 备注：已创建 2 条记录（含 1 条负金额），验证筛选/统计联动与删除后刷新持久化。
